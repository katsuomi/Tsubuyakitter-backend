version: '3.7' # composeファイルのバーション指定
services:
  gin-gorm-twitter-api: # service名
    build: . # ビルドに使用するDockerfileがあるディレクトリ指定
    tty: true # コンテナの起動永続化
    volumes:
      - .:/go/src/github.com/katsuomi/gin-gorm-twitter-api # マウントディレクトリ指定
    ports:
      - "8080:8080"
  db:
    image: postgres:alpine
    environment:
      POSTGRES_USER: gin-gorm-twitter-api
      POSTGRES_PASSWORD: gin-gorm-twitter-api
      POSTGRES_DB: gin-gorm-twitter-api
    ports:
      - 5432:5432